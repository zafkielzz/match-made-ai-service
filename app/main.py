from .api import app
# pip install -r requirements.txt
#  python -m uvicorn app.main:app --host 0.0.0.0 --port 8000  
# python -m uvicorn app.main:app --reload 
#python -m app.parser_cv.test
#pip install python-dotenv
